<%= form_with(model: payment) do |form| %>
  <% if payment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(payment.errors.count, "error") %> prohibited this payment from being saved:</h2>

      <ul>
        <% payment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :contract_id, style: "display: block" %>
    <%= form.text_field :contract_id %>
  </div>

  <div>
    <%= form.label :payment_on_accepted, style: "display: block" %>
    <%= form.number_field :payment_on_accepted %>
  </div>

  <div>
    <%= form.label :payment_on_fulfilled, style: "display: block" %>
    <%= form.number_field :payment_on_fulfilled %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
